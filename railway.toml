[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

# No build secrets needed - Stripe key is runtime only
[build.secrets]
